# Python ignores.
.pytest_cache/
.venv/
venv/
.cache/
.DS_Store
__pycache__/
*.pyc
*.pyo
*.pyd
_version.py
__init__.pyi

# C and CMake ignores.
build/
install/
compile_commands.json
CMakeSettings.json
*egg-info/
cmake-build-*/
prefix/
CMakeLists.txt.user
CMakeUserPresets.json
.ccls

.idea/
.vs/
*.code-workspace
.vscode/launch.json
.luarocks

# Claude and Slang
*.local.json

# Release tarballs built by RELEASE.md step 4 -- rebuildable, never committed.
*.tar.gz

# Per-machine agent and tool state, surfaced at the repo root when this repo was
# flattened out of its old wrapper directory. Local only, never shared.
.claude/
.qwen/
.slang/
